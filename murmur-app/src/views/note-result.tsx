// Murmur — note confirmation view (re-renders contact-card after a note is added).
// All styling via inline styles (MCP App host doesn't load Tailwind).

import ContactCard from "./contact-card.js";

export default function NoteResult() {
  return <ContactCard />;
}
